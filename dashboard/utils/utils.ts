

/**
 * 
 * this app uses generic viem methods to parse between bigint amounts and string amounts
 * 
 * from string to bigint: parseUnits()
 * from bigint to string: formatUnits()
 * 
 */


/**
 * 1. SuccessToast() - shows a loading notification that tracks the txn through hash and returns success
 * 2. ErrorToast() - shows a loading notification that tracks the txn through hash and returns error
 *  
 */

